div(layout='column' layout-padding).passage.md-whiteframe-8dp.mt-20
	form(name="passForm" layout="column")
		md-input-container
			label 标题
			input(ng-model="passage.title" name="title" required)
			div(ng-messages="passForm.title.$error")
				div(ng-message="required") 标题不能为空
		textarea(rows="20" layout-padding required ng-model="passage.content" name="content").content
		md-input-container
			div(ng-messages="passForm.content.$error" ng-show='passForm.content.$dirty').error
				div(ng-message="required") 内容不能为空
		md-button.md-primary.md-accent(ng-click="submit()") 保存
